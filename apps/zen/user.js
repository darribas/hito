// Zen browser preferences.
//
// This file is copied over Zen's profile user.js on every run of setup.sh, so
// anything set here is enforced and anything removed here reverts to Zen's
// default on the next run. Add prefs as `user_pref("name", value);` — see
// about:config in Zen for names and current values.
//
// Left minimal on purpose; fill in as you settle on preferences.
